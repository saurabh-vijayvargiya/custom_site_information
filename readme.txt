Drupal.org is used for reference while developing this module.

This module show node data in json format when node id of page content type is passed along with siteapikey in url.

1.5 hrs were taken to develop this module.